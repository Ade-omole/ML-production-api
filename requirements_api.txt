# Production API Requirements
# This file contains only the dependencies needed to run the inference API

# FastAPI and ASGI server
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Data validation
pydantic==2.5.0

# ML libraries (for loading and running the model)
# Note: Model was saved with scikit-learn 1.8.0 (requires Python 3.11+)
# For Python 3.9, using compatible versions. Model should still load but may show warnings.
scikit-learn>=1.6.0
joblib>=1.3.2
numpy>=2.0.0  # Required for numpy._core module
pandas>=2.1.0  # Required for numpy 2.0 compatibility (scikit-learn imports pandas internally)

# Optional but recommended for production
python-multipart==0.0.6  # For form data parsing if needed

# Testing dependencies (for CI/CD and local testing)
pytest>=7.0.0  # Testing framework
httpx>=0.24.0  # For FastAPI TestClient

